name: nobowa
description: A mobile application which serves as a gateway to Nobowa.com.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  connectivity_plus:
  country_code_picker:
  cloud_firestore:
  firebase_core:
  firebase_phone_auth_handler:
  font_awesome_flutter:
  geocoder:
  intl:
  location:
  path_provider:
  url_launcher:
  webview_flutter:

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0
flutter:

  uses-material-design: true

  assets:
    - assets/images/
